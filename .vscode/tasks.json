{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build and zip publish output",
			"type": "shell",
			"command": "dotnet publish -c Release -o ./publish; Compress-Archive -Path ./publish/* -DestinationPath ./publish.zip -Force"
		}
	]
}